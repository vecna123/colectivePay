import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.VerasCode.colectivepay',
  appName: 'ColectivePay',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
